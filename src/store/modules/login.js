const state = {
  userInfo: {},
};

const getters = {
};

const mutations = {
  setUserInfo (state, userInfo) {
    state.userInfo = userInfo
  },
};

const actions = {
};

export default {
  state,
  getters,
  mutations,
  actions
};
